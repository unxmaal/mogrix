# CANDIDATE RULES — generated by mogrix batch-build
# Review and move to rules/packages/zathura.yaml to activate
#
# Source analysis: 0 files scanned, 5 findings
# Generated: 2026-02-09T01:38:12

package: zathura

rules:
  # confidence: 95% — Known Linux-only BuildRequires
  drop_buildrequires:
    - libseccomp-devel

# TODO (manual review needed):
# - printf-zu: IRIX libc doesn't support %zu/%zd/%zx — use %u/%d/%x (size_t is 4 bytes on n32) (zathura/content-type.c:123)
# - printf-zu: IRIX libc doesn't support %zu/%zd/%zx — use %u/%d/%x (size_t is 4 bytes on n32) (zathura/render.c:1008)

