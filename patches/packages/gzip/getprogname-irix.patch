--- a/lib/getprogname.c
+++ b/lib/getprogname.c
@@ -42,12 +42,6 @@
 # include <string.h>
 #endif

-#if defined __sgi || defined __osf__
-# include <string.h>
-# include <unistd.h>
-# include <stdio.h>
-# include <fcntl.h>
-# include <sys/procfs.h>
-#endif
+/* IRIX procfs includes removed - using simplified implementation below */

 #if defined __SCO_VERSION__ || defined __sysv5__
@@ -234,30 +228,9 @@
     }
   return p;
 # elif defined __sgi || defined __osf__                     /* IRIX or Tru64 */
-  char filename[50];
-  int fd;
-
-  # if defined __sgi
-    sprintf (filename, "/proc/pinfo/%d", (int) getpid ());
-  # else
-    sprintf (filename, "/proc/%d", (int) getpid ());
-  # endif
-  fd = open (filename, O_RDONLY | O_CLOEXEC);
-  if (0 <= fd)
-    {
-      prpsinfo_t buf;
-      int ioctl_ok = 0 <= ioctl (fd, PIOCPSINFO, &buf);
-      close (fd);
-      if (ioctl_ok)
-        {
-          char *name = buf.pr_fname;
-          size_t namesize = sizeof buf.pr_fname;
-          /* It may not be NUL-terminated.  */
-          char *namenul = memchr (name, '\0', namesize);
-          size_t namelen = namenul ? namenul - name : namesize;
-          char *namecopy = malloc (namelen + 1);
-          if (namecopy)
-            {
-              namecopy[namelen] = '\0';
-              return memcpy (namecopy, name, namelen);
-            }
-        }
-    }
+  /* IRIX procfs interface requires types (gregset_t, prpsinfo_t) that
+   * need complex header dependencies. Since getprogname is only used
+   * for error messages, just return NULL to use the fallback.
+   * This matches the SGUG-RSE approach for cross-compilation.
+   */
   return NULL;
 # elif defined __SCO_VERSION__ || defined __sysv5__                /* SCO OpenServer6/UnixWare */
