# CANDIDATE RULES — generated by mogrix batch-build
# Review and move to rules/packages/ffmpeg.yaml to activate
#
# Source analysis: 0 files scanned, 23 findings
# Generated: 2026-02-09T01:39:14

package: ffmpeg

rules:
  # confidence: 95% — Known Linux-only BuildRequires
  drop_buildrequires:
    - texinfo

  # confidence: 95% — SourceAnalyzer detected function usage in C source
  inject_compat_functions:
    - asprintf
    - strerror_r

# TODO (manual review needed):
# - printf-zu: IRIX libc doesn't support %zu/%zd/%zx — use %u/%d/%x (size_t is 4 bytes on n32) (libavformat/ipfsgateway.c:72)
# - printf-zu: IRIX libc doesn't support %zu/%zd/%zx — use %u/%d/%x (size_t is 4 bytes on n32) (doc/examples/avio_read_callback.c:48)
# - printf-zu: IRIX libc doesn't support %zu/%zd/%zx — use %u/%d/%x (size_t is 4 bytes on n32) (libavformat/argo_cvg.c:351)
# - printf-zu: IRIX libc doesn't support %zu/%zd/%zx — use %u/%d/%x (size_t is 4 bytes on n32) (libavfilter/vaapi_vpp.c:596)
# - printf-zu: IRIX libc doesn't support %zu/%zd/%zx — use %u/%d/%x (size_t is 4 bytes on n32) (tools/enc_recon_frame_test.c:359)
# - printf-zu: IRIX libc doesn't support %zu/%zd/%zx — use %u/%d/%x (size_t is 4 bytes on n32) (libavutil/vulkan.c:1422)
# - printf-zu: IRIX libc doesn't support %zu/%zd/%zx — use %u/%d/%x (size_t is 4 bytes on n32) (libavfilter/graphparser.c:548)
# - printf-zu: IRIX libc doesn't support %zu/%zd/%zx — use %u/%d/%x (size_t is 4 bytes on n32) (libavcodec/vaapi_encode_h264.c:129)
# - printf-zu: IRIX libc doesn't support %zu/%zd/%zx — use %u/%d/%x (size_t is 4 bytes on n32) (libavcodec/vaapi_encode_h265.c:115)
# - printf-zu: IRIX libc doesn't support %zu/%zd/%zx — use %u/%d/%x (size_t is 4 bytes on n32) (libavcodec/vaapi_encode_mpeg2.c:83)
# - printf-zu: IRIX libc doesn't support %zu/%zd/%zx — use %u/%d/%x (size_t is 4 bytes on n32) (libavcodec/vaapi_decode.c:56)
# - printf-zu: IRIX libc doesn't support %zu/%zd/%zx — use %u/%d/%x (size_t is 4 bytes on n32) (libavcodec/mediacodec.c:96)
# - printf-zu: IRIX libc doesn't support %zu/%zd/%zx — use %u/%d/%x (size_t is 4 bytes on n32) (libavcodec/vaapi_encode.c:83)
# - printf-zu: IRIX libc doesn't support %zu/%zd/%zx — use %u/%d/%x (size_t is 4 bytes on n32) (libavcodec/vaapi_encode_mjpeg.c:134)
# - printf-zu: IRIX libc doesn't support %zu/%zd/%zx — use %u/%d/%x (size_t is 4 bytes on n32) (libavcodec/vaapi_encode_av1.c:194)

