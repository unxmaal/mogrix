# openjade package rules for IRIX cross-compilation
package: openjade

build_after: none
skip: true
skip_reason: "missing dependency: opensp-devel not staged (no opensp SRPM available)"
blocked: true
blocked_reason: "missing dependency: opensp-devel not staged (no opensp SRPM available)"

# IRIX-specific patches from SGUG-RSE
add_patch:
  - openjade.sgifixes.patch
