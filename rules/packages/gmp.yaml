# gmp - GNU Multiple Precision Arithmetic Library (FC40: 6.2.1)
package: gmp

rules:
  # gmp-impl.h has #define strchr index for non-STDC compilers, but on IRIX
  # index() isn't declared in scope. We need to ensure HAVE_STRING_H is defined
  # so the code path uses strchr directly, not the index() alias.
  # Also force ac_cv_func_obstack_vprintf to avoid configure check failure.
  prep_commands:
    - "sed -i '/#define strchr.*index/d' gmp-impl.h || true"

  drop_buildrequires:
    - texinfo

  configure_flags:
    add:
      - --disable-assembly
    remove:
      - --enable-fat

  drop_buildrequires:
    - fipscheck

  spec_replacements:
    # Disable FIPS (fipshmac not available, not needed for IRIX)
    - pattern: "%bcond_without fips"
      replacement: "%bcond_with fips"
    # Switch from git-based autosetup to simple patch application
    - pattern: "%autosetup -S git -n %{name}-%{version}"
      replacement: "%autosetup -p1 -n %{name}-%{version}"

    # Remove info dir cleanup — remove all info files
    - pattern: "rm -f %{buildroot}%{_infodir}/dir"
      replacement: "rm -rf %{buildroot}%{_infodir}"
    - pattern: "rm -f $RPM_BUILD_ROOT%{_infodir}/dir"
      replacement: "rm -rf $RPM_BUILD_ROOT%{_infodir}"

    # Remove info files from %files
    - pattern: "%{_infodir}/gmp.info*"
      replacement: ""

