package: libarchive
add_patch:
- libarchive.sgifixes.patch
rules:
  drop_buildrequires:
  - e2fsprogs-devel
  configure_flags:
    add:
    - --disable-acl
    - --disable-xattr
    - --without-nettle
    - --without-libb2
    remove:
    - --with-zstd
  configure_disable:
  - acl
  - xattr
