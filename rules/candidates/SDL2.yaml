# CANDIDATE RULES — generated by mogrix batch-build
# Review and move to rules/packages/SDL2.yaml to activate
#
# Source analysis: 0 files scanned, 14 findings
# Generated: 2026-02-09T01:39:09

package: SDL2

rules:
  # confidence: 95% — Known Linux-only BuildRequires
  drop_buildrequires:
    - systemd-devel

  # confidence: 95% — SourceAnalyzer detected function usage in C source
  inject_compat_functions:
    - strdup

# TODO (manual review needed):
# - printf-zu: IRIX libc doesn't support %zu/%zd/%zx — use %u/%d/%x (size_t is 4 bytes on n32) (test/testautomation_stdlib.c:172)
# - thread-local-storage: IRIX rld has no __thread TLS support (src/stdlib/SDL_malloc.c:230)

