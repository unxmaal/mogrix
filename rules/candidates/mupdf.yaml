# CANDIDATE RULES — generated by mogrix batch-build
# Review and move to rules/packages/mupdf.yaml to activate
#
# Source analysis: 0 files scanned, 146 findings
# Generated: 2026-02-09T01:38:11

package: mupdf

rules:
  # confidence: 95% — SourceAnalyzer detected function usage in C source
  inject_compat_functions:
    - asprintf
    - getopt_long
    - strdup
    - strerror_r

# TODO (manual review needed):
# - printf-zu: IRIX libc doesn't support %zu/%zd/%zx — use %u/%d/%x (size_t is 4 bytes on n32) (source/tools/mudraw.c:1579)
# - printf-zu: IRIX libc doesn't support %zu/%zd/%zx — use %u/%d/%x (size_t is 4 bytes on n32) (source/tools/muraster.c:1786)
# - printf-zu: IRIX libc doesn't support %zu/%zd/%zx — use %u/%d/%x (size_t is 4 bytes on n32) (thirdparty/extract/src/memento.c:73)
# - printf-zu: IRIX libc doesn't support %zu/%zd/%zx — use %u/%d/%x (size_t is 4 bytes on n32) (platform/x11/curl_stream.c:162)
# - printf-zu: IRIX libc doesn't support %zu/%zd/%zx — use %u/%d/%x (size_t is 4 bytes on n32) (source/fitz/bidi-std.c:725)
# - printf-zu: IRIX libc doesn't support %zu/%zd/%zx — use %u/%d/%x (size_t is 4 bytes on n32) (source/fitz/memory.c:91)
# - printf-zu: IRIX libc doesn't support %zu/%zd/%zx — use %u/%d/%x (size_t is 4 bytes on n32) (source/fitz/output-pdfocr.c:362)
# - printf-zu: IRIX libc doesn't support %zu/%zd/%zx — use %u/%d/%x (size_t is 4 bytes on n32) (source/pdf/pdf-crypt.c:146)
# - printf-zu: IRIX libc doesn't support %zu/%zd/%zx — use %u/%d/%x (size_t is 4 bytes on n32) (source/fitz/store.c:530)
# - printf-zu: IRIX libc doesn't support %zu/%zd/%zx — use %u/%d/%x (size_t is 4 bytes on n32) (source/fitz/draw-glyph.c:484)
# - printf-zu: IRIX libc doesn't support %zu/%zd/%zx — use %u/%d/%x (size_t is 4 bytes on n32) (thirdparty/curl/src/tool_paramhlp.c:481)
# - printf-zu: IRIX libc doesn't support %zu/%zd/%zx — use %u/%d/%x (size_t is 4 bytes on n32) (source/fitz/output-pclm.c:194)
# - printf-zu: IRIX libc doesn't support %zu/%zd/%zx — use %u/%d/%x (size_t is 4 bytes on n32) (source/fitz/memento.c:104)
# - printf-zu: IRIX libc doesn't support %zu/%zd/%zx — use %u/%d/%x (size_t is 4 bytes on n32) (thirdparty/curl/src/tool_urlglob.c:465)
# - printf-zu: IRIX libc doesn't support %zu/%zd/%zx — use %u/%d/%x (size_t is 4 bytes on n32) (thirdparty/curl/src/tool_cb_dbg.c:149)
# - printf-zu: IRIX libc doesn't support %zu/%zd/%zx — use %u/%d/%x (size_t is 4 bytes on n32) (thirdparty/jbig2dec/jbig2dec.c:102)
# - printf-zu: IRIX libc doesn't support %zu/%zd/%zx — use %u/%d/%x (size_t is 4 bytes on n32) (thirdparty/jbig2dec/memento.c:68)
# - printf-zu: IRIX libc doesn't support %zu/%zd/%zx — use %u/%d/%x (size_t is 4 bytes on n32) (thirdparty/leptonica/src/jpegio.c:926)
# - printf-zu: IRIX libc doesn't support %zu/%zd/%zx — use %u/%d/%x (size_t is 4 bytes on n32) (thirdparty/leptonica/src/pixcomp.c:1746)
# - printf-zu: IRIX libc doesn't support %zu/%zd/%zx — use %u/%d/%x (size_t is 4 bytes on n32) (thirdparty/leptonica/src/tiffio.c:2197)
# - printf-zu: IRIX libc doesn't support %zu/%zd/%zx — use %u/%d/%x (size_t is 4 bytes on n32) (thirdparty/leptonica/src/utils1.c:620)
# - printf-zu: IRIX libc doesn't support %zu/%zd/%zx — use %u/%d/%x (size_t is 4 bytes on n32) (thirdparty/leptonica/src/sarray1.c:2002)
# - printf-zu: IRIX libc doesn't support %zu/%zd/%zx — use %u/%d/%x (size_t is 4 bytes on n32) (thirdparty/leptonica/src/webpanimio.c:266)
# - printf-zu: IRIX libc doesn't support %zu/%zd/%zx — use %u/%d/%x (size_t is 4 bytes on n32) (thirdparty/leptonica/src/utils2.c:1535)
# - printf-zu: IRIX libc doesn't support %zu/%zd/%zx — use %u/%d/%x (size_t is 4 bytes on n32) (thirdparty/leptonica/src/spixio.c:450)
# - printf-zu: IRIX libc doesn't support %zu/%zd/%zx — use %u/%d/%x (size_t is 4 bytes on n32) (thirdparty/leptonica/src/pdfio2.c:723)
# - printf-zu: IRIX libc doesn't support %zu/%zd/%zx — use %u/%d/%x (size_t is 4 bytes on n32) (thirdparty/leptonica/src/zlibmem.c:145)
# - printf-zu: IRIX libc doesn't support %zu/%zd/%zx — use %u/%d/%x (size_t is 4 bytes on n32) (thirdparty/leptonica/src/pixalloc.c:418)
# - printf-zu: IRIX libc doesn't support %zu/%zd/%zx — use %u/%d/%x (size_t is 4 bytes on n32) (thirdparty/leptonica/src/regutils.c:335)
# - printf-zu: IRIX libc doesn't support %zu/%zd/%zx — use %u/%d/%x (size_t is 4 bytes on n32) (thirdparty/curl/lib/transfer.c:320)
# - printf-zu: IRIX libc doesn't support %zu/%zd/%zx — use %u/%d/%x (size_t is 4 bytes on n32) (thirdparty/curl/lib/socks.c:399)
# - printf-zu: IRIX libc doesn't support %zu/%zd/%zx — use %u/%d/%x (size_t is 4 bytes on n32) (thirdparty/tesseract/src/classify/shapetable.h:50)
# - printf-zu: IRIX libc doesn't support %zu/%zd/%zx — use %u/%d/%x (size_t is 4 bytes on n32) (thirdparty/curl/lib/http.c:3202)
# - printf-zu: IRIX libc doesn't support %zu/%zd/%zx — use %u/%d/%x (size_t is 4 bytes on n32) (thirdparty/curl/lib/sendf.c:550)
# - printf-zu: IRIX libc doesn't support %zu/%zd/%zx — use %u/%d/%x (size_t is 4 bytes on n32) (thirdparty/curl/lib/memdebug.c:182)
# - printf-zu: IRIX libc doesn't support %zu/%zd/%zx — use %u/%d/%x (size_t is 4 bytes on n32) (thirdparty/curl/lib/security.c:486)
# - printf-zu: IRIX libc doesn't support %zu/%zd/%zx — use %u/%d/%x (size_t is 4 bytes on n32) (thirdparty/curl/lib/conncache.c:303)
# - printf-zu: IRIX libc doesn't support %zu/%zd/%zx — use %u/%d/%x (size_t is 4 bytes on n32) (thirdparty/curl/lib/rtsp.c:676)
# - printf-zu: IRIX libc doesn't support %zu/%zd/%zx — use %u/%d/%x (size_t is 4 bytes on n32) (thirdparty/curl/lib/http2.c:722)
# - printf-zu: IRIX libc doesn't support %zu/%zd/%zx — use %u/%d/%x (size_t is 4 bytes on n32) (thirdparty/curl/lib/url.c:788)
# - printf-zu: IRIX libc doesn't support %zu/%zd/%zx — use %u/%d/%x (size_t is 4 bytes on n32) (thirdparty/curl/lib/vquic/ngtcp2.c:728)
# - printf-zu: IRIX libc doesn't support %zu/%zd/%zx — use %u/%d/%x (size_t is 4 bytes on n32) (thirdparty/curl/lib/imap.c:1155)
# - printf-zu: IRIX libc doesn't support %zu/%zd/%zx — use %u/%d/%x (size_t is 4 bytes on n32) (thirdparty/curl/lib/vtls/schannel.c:845)
# - printf-zu: IRIX libc doesn't support %zu/%zd/%zx — use %u/%d/%x (size_t is 4 bytes on n32) (thirdparty/curl/lib/vquic/quiche.c:343)
# - printf-zu: IRIX libc doesn't support %zu/%zd/%zx — use %u/%d/%x (size_t is 4 bytes on n32) (thirdparty/curl/lib/cookie.c:501)
# - printf-zu: IRIX libc doesn't support %zu/%zd/%zx — use %u/%d/%x (size_t is 4 bytes on n32) (thirdparty/curl/lib/pingpong.c:422)
# - printf-zu: IRIX libc doesn't support %zu/%zd/%zx — use %u/%d/%x (size_t is 4 bytes on n32) (thirdparty/curl/lib/vtls/sectransp.c:981)
# - printf-zu: IRIX libc doesn't support %zu/%zd/%zx — use %u/%d/%x (size_t is 4 bytes on n32) (thirdparty/curl/lib/multi.c:573)

