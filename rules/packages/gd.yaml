# gd package rules for IRIX cross-compilation
package: gd

build_after: none
skip: true
skip_reason: "no SRPM in mogrix_inputs"
blocked: true
blocked_reason: "no SRPM in mogrix_inputs"

# IRIX-specific patches from SGUG-RSE
add_patch:
  - libgd.sgifixes.patch

