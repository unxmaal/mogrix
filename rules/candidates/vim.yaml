# CANDIDATE RULES — generated by mogrix batch-build
# Review and move to rules/packages/vim.yaml to activate
#
# Source analysis: 0 files scanned, 14 findings
# Generated: 2026-02-09T01:37:19

package: vim

rules:
  # confidence: 95% — Known Linux-only BuildRequires
  drop_buildrequires:
    - libselinux-devel
    - perl-generators

  # confidence: 95% — SourceAnalyzer detected function usage in C source
  inject_compat_functions:
    - getline
    - strdup

# TODO (manual review needed):
# - printf-zu: IRIX libc doesn't support %zu/%zd/%zx — use %u/%d/%x (size_t is 4 bytes on n32) (src/libvterm/src/encoding.c:42)
# - thread-local-storage: IRIX rld has no __thread TLS support (src/if_mzsch.c:932)
# - thread-local-storage: IRIX rld has no __thread TLS support (src/if_mzsch.h:15)

