# CANDIDATE RULES — generated by mogrix batch-build
# Review and move to rules/packages/profanity.yaml to activate
#
# Source analysis: 0 files scanned, 79 findings
# Generated: 2026-02-09T01:37:01

package: profanity

rules:
  # confidence: 95% — SourceAnalyzer detected function usage in C source
  inject_compat_functions:
    - getline
    - strdup

# TODO (manual review needed):
# - printf-zu: IRIX libc doesn't support %zu/%zd/%zx — use %u/%d/%x (size_t is 4 bytes on n32) (src/config/color.c:374)
# - printf-zu: IRIX libc doesn't support %zu/%zd/%zx — use %u/%d/%x (size_t is 4 bytes on n32) (src/ui/window.c:1200)

