# CANDIDATE RULES — generated by mogrix batch-build
# Review and move to rules/packages/qtbase.yaml to activate
#
# Source analysis: 0 files scanned, 15 findings
# Generated: 2026-02-09T01:39:03

package: qtbase

rules:
  # confidence: 95% — Known Linux-only BuildRequires
  drop_buildrequires:
    - perl-generators

  # confidence: 95% — SourceAnalyzer detected function usage in C source
  inject_compat_functions:
    - asprintf
    - secure_getenv
    - strdup
    - strerror_r

# TODO (manual review needed):
# - printf-zu: IRIX libc doesn't support %zu/%zd/%zx — use %u/%d/%x (size_t is 4 bytes on n32) (src/3rdparty/VulkanMemoryAllocator/vk_mem_alloc.h:14243)
# - printf-zu: IRIX libc doesn't support %zu/%zd/%zx — use %u/%d/%x (size_t is 4 bytes on n32) (src/3rdparty/pcre2/src/pcre2_compile.c:958)

