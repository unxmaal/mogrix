# neofetch - system information tool (bash script)
# Uses make with PREFIX — needs override for /opt/mogrix
package: neofetch

build_after: none

rules:
  spec_replacements:
    - pattern: "%make_install"
      replacement: "make install DESTDIR=%{buildroot} PREFIX=%{_prefix} MANDIR=%{_mandir}"
