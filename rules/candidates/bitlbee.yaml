# CANDIDATE RULES — generated by mogrix batch-build
# Review and move to rules/packages/bitlbee.yaml to activate
#
# Source analysis: 0 files scanned, 9 findings
# Generated: 2026-02-09T01:37:01

package: bitlbee

rules:
  # confidence: 95% — Known Linux-only BuildRequires
  drop_buildrequires:
    - systemd-rpm-macros

  # confidence: 95% — SourceAnalyzer detected function usage in C source
  inject_compat_functions:
    - strdup

# TODO (manual review needed):
# - printf-zu: IRIX libc doesn't support %zu/%zd/%zx — use %u/%d/%x (size_t is 4 bytes on n32) (conf.c:326)
# - printf-zu: IRIX libc doesn't support %zu/%zd/%zx — use %u/%d/%x (size_t is 4 bytes on n32) (protocols/jabber/si.c:174)
# - printf-zu: IRIX libc doesn't support %zu/%zd/%zx — use %u/%d/%x (size_t is 4 bytes on n32) (dcc.c:125)
# - printf-zu: IRIX libc doesn't support %zu/%zd/%zx — use %u/%d/%x (size_t is 4 bytes on n32) (root_commands.c:1547)
# - printf-zu: IRIX libc doesn't support %zu/%zd/%zx — use %u/%d/%x (size_t is 4 bytes on n32) (lib/oauth2.c:94)
# - printf-zu: IRIX libc doesn't support %zu/%zd/%zx — use %u/%d/%x (size_t is 4 bytes on n32) (lib/oauth.c:261)
# - printf-zu: IRIX libc doesn't support %zu/%zd/%zx — use %u/%d/%x (size_t is 4 bytes on n32) (protocols/twitter/twitter_http.c:119)

