--- a/unix/tcl.m4
+++ b/unix/tcl.m4
@@ -1350,7 +1350,8 @@
 	    ;;
 	IRIX64-6.*)
 	    SHLIB_CFLAGS=""
-	    SHLIB_LD="ld -n32 -shared -rdata_shared"
+	    # Use CC wrapper for cross-compilation (raw ld is the host linker)
+	    SHLIB_LD='${CC} -shared'
 	    SHLIB_SUFFIX=".so"
 	    DL_OBJS="tclLoadDl.o"
 	    DL_LIBS=""
