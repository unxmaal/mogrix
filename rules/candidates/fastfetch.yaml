# CANDIDATE RULES — generated by mogrix batch-build
# Review and move to rules/packages/fastfetch.yaml to activate
#
# Source analysis: 0 files scanned, 12 findings
# Generated: 2026-02-09T01:39:42

package: fastfetch

rules:
  # confidence: 95% — Known Linux-only BuildRequires
  drop_buildrequires:
    - dbus-devel

  # confidence: 95% — SourceAnalyzer detected function usage in C source
  inject_compat_functions:
    - asprintf
    - getline

# TODO (manual review needed):
# - printf-zu: IRIX libc doesn't support %zu/%zd/%zx — use %u/%d/%x (size_t is 4 bytes on n32) (src/fastfetch.c:335)

