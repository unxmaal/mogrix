# CANDIDATE RULES — generated by mogrix batch-build
# Review and move to rules/packages/htop.yaml to activate
#
# Source analysis: 0 files scanned, 9 findings
# Generated: 2026-02-09T01:38:14

package: htop

rules:
  # confidence: 95% — Known Linux-only BuildRequires
  drop_buildrequires:
    - libcap-devel

  # confidence: 95% — SourceAnalyzer detected function usage in C source
  inject_compat_functions:
    - asprintf
    - getopt_long
    - strdup

# TODO (manual review needed):
# - printf-zu: IRIX libc doesn't support %zu/%zd/%zx — use %u/%d/%x (size_t is 4 bytes on n32) (CRT.c:891)
# - printf-zu: IRIX libc doesn't support %zu/%zd/%zx — use %u/%d/%x (size_t is 4 bytes on n32) (Hashtable.c:43)
# - printf-zu: IRIX libc doesn't support %zu/%zd/%zx — use %u/%d/%x (size_t is 4 bytes on n32) (CategoriesPanel.c:51)

