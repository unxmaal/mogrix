# CANDIDATE RULES — generated by mogrix batch-build
# Review and move to rules/packages/man-db.yaml to activate
#
# Source analysis: 0 files scanned, 31 findings
# Generated: 2026-02-09T01:36:57

package: man-db
classes: ['nls-disabled']

rules:
  # confidence: 95% — Known Linux-only BuildRequires
  drop_buildrequires:
    - systemd

  # confidence: 95% — SourceAnalyzer detected function usage in C source
  inject_compat_functions:
    - asprintf
    - getline
    - getopt_long
    - reallocarray
    - strdup
    - strerror_r

# TODO (manual review needed):
# - printf-zu: IRIX libc doesn't support %zu/%zd/%zx — use %u/%d/%x (size_t is 4 bytes on n32) (libdb/db_lookup.c:256)
# - printf-zu: IRIX libc doesn't support %zu/%zd/%zx — use %u/%d/%x (size_t is 4 bytes on n32) (src/catman.c:221)
# - printf-zu: IRIX libc doesn't support %zu/%zd/%zx — use %u/%d/%x (size_t is 4 bytes on n32) (gl/lib/malloc/dynarray_at_failure.c:33)

