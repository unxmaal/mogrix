# CANDIDATE RULES — generated by mogrix batch-build
# Review and move to rules/packages/libxkbcommon.yaml to activate
#
# Source analysis: 0 files scanned, 28 findings
# Generated: 2026-02-09T01:38:23

package: libxkbcommon

rules:
  # confidence: 95% — SourceAnalyzer detected function usage in C source
  inject_compat_functions:
    - asprintf
    - getopt_long
    - secure_getenv
    - strdup

# TODO (manual review needed):
# - printf-zu: IRIX libc doesn't support %zu/%zd/%zx — use %u/%d/%x (size_t is 4 bytes on n32) (src/scanner-utils.h:62)
# - printf-zu: IRIX libc doesn't support %zu/%zd/%zx — use %u/%d/%x (size_t is 4 bytes on n32) (test/atom.c:98)

